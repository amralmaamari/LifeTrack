
                GO
              CREATE PROCEDURE SP_AddNewArticles
(
    @ArticleID int OUTPUT,
    @Title nvarchar(100),
    @Description nvarchar(100),
    @UserID int,
    @CreatedAt datetime,
    @UpdatedAt datetime
)
AS
BEGIN
    SET NOCOUNT ON;
    BEGIN TRY
        INSERT INTO Articles (
            Title,
            Description,
            UserID,
            CreatedAt,
            UpdatedAt
        ) VALUES (
            @Title,
            @Description,
            @UserID,
            @CreatedAt,
            @UpdatedAt
        );
        SELECT @ArticleID = SCOPE_IDENTITY();
    END TRY
    BEGIN CATCH
        DECLARE @ErrorMessage NVARCHAR(4000), @ErrorSeverity INT, @ErrorState INT;
        SELECT
            @ErrorMessage = ERROR_MESSAGE(),
            @ErrorSeverity = ERROR_SEVERITY(),
            @ErrorState = ERROR_STATE();
        RAISERROR (@ErrorMessage, @ErrorSeverity, @ErrorState);
        SET @ArticleID = NULL;
    END CATCH;
END;


                GO
              CREATE PROCEDURE SP_DeleteArticles
(
    @ArticleID int
)
AS
BEGIN
    SET NOCOUNT ON;
    BEGIN TRY
        DELETE FROM Articles WHERE ArticleID = @ArticleID;
    END TRY
    BEGIN CATCH
        DECLARE @ErrorMessage NVARCHAR(4000), @ErrorSeverity INT, @ErrorState INT;
        SELECT
            @ErrorMessage = ERROR_MESSAGE(),
            @ErrorSeverity = ERROR_SEVERITY(),
            @ErrorState = ERROR_STATE();
        RAISERROR (@ErrorMessage, @ErrorSeverity, @ErrorState);
    END CATCH;
END;
 

                GO
              CREATE PROCEDURE SP_GetArticlesInfoByID
(
    @ArticleID int
)
AS
BEGIN
    SET NOCOUNT ON;
    BEGIN TRY
        select * FROM Articles WHERE ArticleID = @ArticleID;
    END TRY
    BEGIN CATCH
        DECLARE @ErrorMessage NVARCHAR(4000), @ErrorSeverity INT, @ErrorState INT;
        SELECT
            @ErrorMessage = ERROR_MESSAGE(),
            @ErrorSeverity = ERROR_SEVERITY(),
            @ErrorState = ERROR_STATE();
        RAISERROR (@ErrorMessage, @ErrorSeverity, @ErrorState);
    END CATCH;
END;


                GO
              
        CREATE PROCEDURE SP_IsArticlesExistsByArticleID
        (
            @ArticleID int
        )
        AS
        BEGIN
            SET NOCOUNT ON;
            BEGIN TRY
                IF EXISTS (SELECT 1 FROM Articles WHERE ArticleID = @ArticleID)
                BEGIN 
                    RETURN 1;
                END
                ELSE
                BEGIN
                    RETURN 0;
                END
            END TRY
            BEGIN CATCH
                DECLARE @ErrorMessage NVARCHAR(4000), @ErrorSeverity INT, @ErrorState INT;
                SELECT 
                    @ErrorMessage = ERROR_MESSAGE(),
                    @ErrorSeverity = ERROR_SEVERITY(),
                    @ErrorState = ERROR_STATE();
                RAISERROR (@ErrorMessage, @ErrorSeverity, @ErrorState);
            END CATCH;
        END;

                GO
              CREATE PROCEDURE SP_UpdateArticlesByID
    @ArticleID int,
    @Title nvarchar (100),
    @Description nvarchar (100),
    @UserID int,
    @CreatedAt datetime,
    @UpdatedAt datetime  AS
BEGIN
    SET NOCOUNT ON;
    BEGIN TRY
    UPDATE Articles
    SET
        Title = @Title,
        Description = @Description,
        UserID = @UserID,
        CreatedAt = @CreatedAt,
        UpdatedAt = @UpdatedAt    WHERE ArticleID = @ArticleID;
    END TRY
    BEGIN CATCH
        SELECT
            ERROR_NUMBER() AS ErrorNumber,
            ERROR_STATE() AS ErrorState,
            ERROR_SEVERITY() AS ErrorSeverity,
            ERROR_PROCEDURE() AS ErrorProcedure,
            ERROR_LINE() AS ErrorLine,
            ERROR_MESSAGE() AS ErrorMessage;
    END CATCH;
END;

                        
     }
 
 